# Agent runtime logs

Codex hooks and the OpenCode plugin write local JSONL runtime logs here by default.

This directory is ignored by Git except for this README. Keep runtime logs local unless you intentionally need to share a sanitized excerpt.
