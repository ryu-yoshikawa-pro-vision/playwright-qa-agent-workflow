# Release QA Cockpit 実装対象ディレクトリ方針

このファイルは `Release QA Cockpit` の実装先を固定する。

実装先は `demo-apps/release-qa-cockpit/` とする。

`examples/release-qa-cockpit/` は古い配置案であり、実装先、生成テストパス、Target Project Profile のいずれにも使用しない。
